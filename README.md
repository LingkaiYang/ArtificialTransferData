# ArtificialTransferData

Artificially generated data from three domains (with different data distribution of two attributes in three classes).
This dataset is used to evaluate domain adaptation methods.
